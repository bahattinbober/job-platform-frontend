import { Reveal } from "@/components/nod/Reveal";

export function BridgeStatement() {
  return (
    <section className="mx-auto w-full max-w-[860px] px-5 py-28 text-center sm:px-8">
      <Reveal>
        <p className="mb-5 font-mono text-[11px] uppercase tracking-[0.18em] text-muted">
          How it works
        </p>
        <h2 className="mx-auto max-w-[26ch] font-display text-[clamp(26px,4.4vw,40px)] font-semibold leading-[1.16] tracking-[-0.02em] [font-variation-settings:'wdth'_86,'opsz'_46]">
          That referral came from six ordinary steps, done in order, without
          you doing any of the networking yourself.
        </h2>
      </Reveal>
    </section>
  );
}
